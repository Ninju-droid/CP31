#include <bits/stdc++.h>
using namespace std;
# define ll long long 

void fun(){
  ll w,h;
  cin>>w>>h;
  ll l;
  cin>>l;
  vector<ll>s1(l);
  ll area = 0;
  for(ll i=0;i<l;i++)cin>>s1[i];
  area = (s1[l-1]-s1[0])*h;
  cin>>l;
  vector<ll>s2(l);
  for(ll i=0;i<l;i++)cin>>s2[i];
  area = max(area,(s2[l-1]-s2[0])*h);

  cin>>l;
  vector<ll>s3(l);
  for(ll i=0;i<l;i++)cin>>s3[i];
  area = max(area,(s3[l-1]-s3[0])*w);
  
  cin>>l;
  vector<ll>s4(l);
  for(ll i=0;i<l;i++)cin>>s4[i];
  area = max(area,(s4[l-1]-s4[0])*w);

cout<<area<<endl;
}
int main() {
#ifndef ONLINE_JUDGE
    freopen("Input.txt", "r", stdin);
    freopen("Output.txt", "w", stdout);
#endif
    int t;
    cin >> t;
    while (t--) fun();
    return 0;
}

