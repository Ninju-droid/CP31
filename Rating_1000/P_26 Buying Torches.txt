#include <bits/stdc++.h>
using namespace std;
#define ll long long 

void fun(){ 
  ll x,y,k;
  cin>>x>>y>>k;
  ll cnt = (k*(1+y)+x-3)/(x-1);
  cnt+=k;
  cout<<cnt<<endl;
}

int main() {
#ifndef ONLINE_JUDGE
    freopen("Input.txt", "r", stdin);
    freopen("Output.txt", "w", stdout);
#endif
    int t;
    cin >> t;
    while (t--) fun();
    return 0;
}
