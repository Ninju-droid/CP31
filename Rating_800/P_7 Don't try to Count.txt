#include <bits/stdc++.h>
using namespace std;

void fun() {
  string x,s;
  int n,m;
  cin>>n>>m;
  cin>>x>>s;
  for(int i=0;i<6;i++){
    if(x.find(s)!= -1){
        cout<<i<<endl;
        return;
    }
    else x+=x;
  }
  cout<<-1<<endl;
}

int main() {
#ifndef ONLINE_JUDGE
    freopen("Input.txt", "r", stdin);
    freopen("Output.txt", "w", stdout);
#endif
    int t;
    cin >> t;
    while (t--) fun();
    return 0;
}