#include <bits/stdc++.h>
using namespace std;
#define ll long long 

void fun(){
  string s;
  cin>>s;
  ll n = s.size();
 if(s[0]==s[n-1])cout<<s<<endl;
 else {s[0]=s[n-1];
  cout<<s<<endl;}
}

int main() {
#ifndef ONLINE_JUDGE
    freopen("Input.txt", "r", stdin);
    freopen("Output.txt", "w", stdout);
#endif
    int t;
    cin >> t;
    while (t--) fun();
    return 0;
}

