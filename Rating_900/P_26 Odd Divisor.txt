#include <bits/stdc++.h>
using namespace std;
#define ll long long 

void fun(){
  ll n;
  cin>>n;
  while(n){
    if(n%2==1 && n>=3){
        cout<<"yes"<<endl;
        return;
    }
    n/=2;
  }
  cout<<"no"<<endl;
}

int main() {
#ifndef ONLINE_JUDGE
    freopen("Input.txt", "r", stdin);
    freopen("Output.txt", "w", stdout);
#endif
    int t;
    cin >> t;
    while (t--) fun();
    return 0;
}

